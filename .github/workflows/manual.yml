name: Manual workflow

on:
  push:
    branches:
    - main
    
jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
    - name: Send greeting
      run: echo "${{ github.event.commits[0][author] }}"
