name: Manual workflow

on:
  push:
    branches:
    - main
    
jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
    - name: Send greeting
      run: IFS='n/' read -a array <<< "${{github.event.head_commit.message}}"; echo "${array[1]}"
