name: Manual workflow

on:
  push:
    branches:
    - main
    
jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
    - name: Send greeting
      run: echo "${{toJSON(github.event.commits[0])}}"
