name: Manual workflow

on:
  push:
    branches:
    - main
    
jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
    - name: Send greeting
      run: IFS=', ' read -a array <<< "Paris, France, Europe"; echo "${array[0]}"
